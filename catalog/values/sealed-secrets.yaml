# Helm values for sealed-secrets (bitnami-labs/sealed-secrets)
# Homelab defaults: single replica, modest resources

replicaCount: 1

resources:
  requests:
    memory: 32Mi
    cpu: 10m
  limits:
    memory: 128Mi
    cpu: 100m
