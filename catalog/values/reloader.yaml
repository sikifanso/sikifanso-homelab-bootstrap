# Helm values for reloader (stakater/reloader)
# Homelab defaults: single replica, modest resources

reloader:
  replicaCount: 1
  resources:
    requests:
      memory: 64Mi
      cpu: 25m
    limits:
      memory: 128Mi
      cpu: 100m
