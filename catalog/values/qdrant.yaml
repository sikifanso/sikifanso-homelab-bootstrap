# Helm values for Qdrant vector database
# Local-first defaults: single replica, persistence enabled

replicaCount: 1

resources:
  requests:
    memory: 256Mi
    cpu: 100m
  limits:
    memory: 1Gi
    cpu: 500m

persistence:
  enabled: true
  size: 10Gi
  accessModes:
    - ReadWriteOnce
