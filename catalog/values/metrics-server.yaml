# Helm values for metrics-server (metrics-server/metrics-server)
# Homelab defaults: single replica, modest resources

replicas: 1

resources:
  requests:
    memory: 64Mi
    cpu: 25m
  limits:
    memory: 128Mi
    cpu: 100m
