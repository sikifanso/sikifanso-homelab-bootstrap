name: redis
category: dev
description: Valkey in-memory data store (Redis-compatible fork)
repoURL: https://valkey.io/valkey-helm/
chart: valkey
targetRevision: ">=0.5.0 <1.0.0"
namespace: dev
enabled: false
